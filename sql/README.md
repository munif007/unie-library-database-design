# SQL Scripts

This folder contains the Microsoft SQL Server implementation of the UNIE Library database.

The main SQL script creates the database schema, inserts sample data, implements triggers, and includes reporting queries.
